import React from "react";

const Header: React.FC = () => {
  return (
    <header className="flex justify-center">
      <p className="text-[22px] font-semibold uppercase text-teal-500">
        taskify
      </p>
    </header>
  );
};

export default Header;
